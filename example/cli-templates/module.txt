import { MainState } from '@/store/main-state';
import { Module } from 'vuex';
import { {{SINGULAR_PASCAL_CASE}} } from './{{SINGULAR_KEBAB_CASE}}';
import { {{SINGULAR_PASCAL_CASE}}State } from './{{SINGULAR_KEBAB_CASE}}-state';

const storeModuleFactory: (main: MainState) => Module<{{SINGULAR_PASCAL_CASE}}State, MainState> = (main) => {
    return {
        namespaced: true,

        state: new {{SINGULAR_PASCAL_CASE}}State(main),

        getters: {
            {{PLURAL_CAMEL_CASE}}: (context) => context.{{PLURAL_CAMEL_CASE}}
        },

        mutations: {
            {{PLURAL_CAMEL_CASE}}: (context, payload) => {
                context.{{PLURAL_CAMEL_CASE}} = payload;
            }
        },

        actions: {

        }
    };
};

export default storeModuleFactory;
